# NVH DataViewer Backend

## Build Setup

``` bash
# create virtualenv
virtualenv venv

# run virtualenv
source venv/bin/activate

# install requirements
pip install -r requirements.txt

#run server
python manage.py runserver

```
